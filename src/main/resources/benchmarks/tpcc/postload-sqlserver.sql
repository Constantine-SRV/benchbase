-- PRIMARY KEYS
ALTER TABLE [dbo].[WAREHOUSE] ADD CONSTRAINT PK_WAREHOUSE 
PRIMARY KEY CLUSTERED (W_ID) 
WITH (DATA_COMPRESSION = PAGE, FILLFACTOR = 90, PAD_INDEX = ON, ONLINE = OFF, SORT_IN_TEMPDB = ON);

ALTER TABLE [dbo].[STOCK] ADD CONSTRAINT PK_STOCK 
PRIMARY KEY CLUSTERED (S_W_ID, S_I_ID) 
WITH (DATA_COMPRESSION = PAGE, FILLFACTOR = 90, PAD_INDEX = ON, ONLINE = OFF, SORT_IN_TEMPDB = ON);

ALTER TABLE [dbo].[ORDER_LINE] ADD CONSTRAINT PK_ORDER_LINE 
PRIMARY KEY CLUSTERED (OL_W_ID, OL_D_ID, OL_O_ID, OL_NUMBER) 
WITH (DATA_COMPRESSION = PAGE, FILLFACTOR = 90, PAD_INDEX = ON, ONLINE = OFF, SORT_IN_TEMPDB = ON);

ALTER TABLE [dbo].[OORDER] ADD CONSTRAINT PK_OORDER 
PRIMARY KEY CLUSTERED (O_W_ID, O_D_ID, O_ID) 
WITH (DATA_COMPRESSION = PAGE, FILLFACTOR = 90, PAD_INDEX = ON, ONLINE = OFF, SORT_IN_TEMPDB = ON);

ALTER TABLE [dbo].[NEW_ORDER] ADD CONSTRAINT PK_NEW_ORDER 
PRIMARY KEY CLUSTERED (NO_W_ID, NO_D_ID, NO_O_ID) 
WITH (DATA_COMPRESSION = PAGE, FILLFACTOR = 90, PAD_INDEX = ON, ONLINE = OFF, SORT_IN_TEMPDB = ON);

ALTER TABLE [dbo].[ITEM] ADD CONSTRAINT PK_ITEM 
PRIMARY KEY CLUSTERED (I_ID) 
WITH (DATA_COMPRESSION = PAGE, FILLFACTOR = 90, PAD_INDEX = ON, ONLINE = OFF, SORT_IN_TEMPDB = ON);

ALTER TABLE [dbo].[DISTRICT] ADD CONSTRAINT PK_DISTRICT 
PRIMARY KEY CLUSTERED (D_W_ID, D_ID) 
WITH (DATA_COMPRESSION = PAGE, FILLFACTOR = 90, PAD_INDEX = ON, ONLINE = OFF, SORT_IN_TEMPDB = ON);

ALTER TABLE [dbo].[CUSTOMER] ADD CONSTRAINT PK_CUSTOMER 
PRIMARY KEY CLUSTERED (C_W_ID, C_D_ID, C_ID) 
WITH (DATA_COMPRESSION = PAGE, FILLFACTOR = 90, PAD_INDEX = ON, ONLINE = OFF, SORT_IN_TEMPDB = ON);

-- UNIQUE CONSTRAINT
ALTER TABLE [dbo].[OORDER] ADD CONSTRAINT UQ_OORDER 
UNIQUE NONCLUSTERED (O_W_ID, O_D_ID, O_C_ID, O_ID) 
WITH (DATA_COMPRESSION = PAGE, FILLFACTOR = 90, PAD_INDEX = ON, ONLINE = OFF, SORT_IN_TEMPDB = ON);

-- CREATE INDEXES
CREATE INDEX IDX_CUSTOMER_NAME ON [dbo].[CUSTOMER] (C_W_ID, C_D_ID, C_LAST, C_FIRST) 
WITH (DATA_COMPRESSION = PAGE, FILLFACTOR = 90, PAD_INDEX = ON, ONLINE = OFF, SORT_IN_TEMPDB = ON);
